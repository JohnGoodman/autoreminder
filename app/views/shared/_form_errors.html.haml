- if errors.any?
  #error_explanation
    %h2
      = pluralize(errors.count, "error")
      prohibited this #{controller.controller_name.singularize} from being saved:
    %ul
      - errors.full_messages.each do |msg|
        %li= msg