= form_for @service_reminder do |f|
  -if @service_reminder.errors.any?
    #error_explanation
      %h2= "#{pluralize(@service_reminder.errors.count, "error")} prohibited this service_reminder from being saved:"
      %ul
        - @service_reminder.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .field
    = f.label :description
    = f.text_area :description
  .field
    = f.label :email_subject
    = f.text_field :email_subject
  .field
    = f.label :email_body
    = f.text_field :email_body
  .actions
    = f.submit 'Save'
